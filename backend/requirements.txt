# Core runtime dependencies for the Neryva API + worker.
# Install with: pip install -r requirements.txt
fastapi>=0.115
uvicorn>=0.30
pydantic>=2.10
pydantic-settings>=2.0
python-dotenv>=1.0
structlog>=24.0
sqlalchemy>=2.0
alembic>=1.13
aiosqlite>=0.20
asyncpg>=0.30
langgraph>=1.2
redis>=5.0
httpx>=0.27
aiohttp>=3.9
jsonschema>=4.0
pgvector>=0.3

# Optional (fail-closed at runtime when absent; add for production features):
# openai>=1.0            # OpenAI provider
# anthropic>=0.30        # Anthropic provider
# sentence-transformers>=2.7  # real embeddings for ingestion.embed
# torch>=2.4
# presidio-analyzer>=2.2.362  # PII redaction
# presidio-anonymizer>=2.2.362
# nemoguardrails>=0.23.0      # NeMo guardrails
# guardrails-ai==0.10.0       # GuardrailsAI rails
# langfuse>=3.0               # observability
